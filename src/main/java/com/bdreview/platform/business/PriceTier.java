package com.bdreview.platform.business;

/** $ / $$ / $$$ / $$$$ style tier, filterable in search (spec §1, §3). */
public enum PriceTier {
    BUDGET,
    MODERATE,
    EXPENSIVE,
    VERY_EXPENSIVE
}
